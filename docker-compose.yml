version: '2'
services:
  tasklist:
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/tasklist
    ports:
      - "6789:6789"
  front:
    restart: always
    build:
      context: .
      dockerfile: Dockerfile-static
    volumes:
      - front:/front
    ports:
      - "6788:6788"
